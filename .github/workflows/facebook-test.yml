name: Test OrbeStation Facebook

on:
  workflow_dispatch:
  push:

jobs:
  test-facebook:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Install Playwright
        run: |
          python -m pip install --upgrade pip
          pip install playwright
          python -m playwright install --with-deps chromium

      - name: Test Facebook page
        run: |
          python - <<'PY'
          from playwright.sync_api import sync_playwright

          with sync_playwright() as p:
              browser = p.chromium.launch(headless=True)
              page = browser.new_page()

              response = page.goto(
                  "https://www.facebook.com/OrbeStation/",
                  wait_until="domcontentloaded",
                  timeout=60000
              )

              page.wait_for_timeout(5000)

              print("STATUS:", response.status if response else "NO RESPONSE")
              print("URL:", page.url)
              print("TITLE:", page.title())
              print("TEXT:")
              print(page.locator("body").inner_text(timeout=30000)[:5000])

              page.screenshot(path="facebook-test.png")

              browser.close()

      - name: Save screenshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: facebook-test-screenshot
          path: facebook-test.png
          if-no-files-found: ignore
